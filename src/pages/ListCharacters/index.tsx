import BaseLayout from '@/layouts/BaseLayout';
import RootLayout from '@/layouts/Root';

const ListCharactersPage: React.FC = () => {
  return (
    <RootLayout>
      <BaseLayout>LIST CHARACTERS</BaseLayout>
    </RootLayout>
  );
};

export default ListCharactersPage;
